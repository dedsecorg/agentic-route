# Smithery configuration: https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    description: Kernel policy-routing lock and net-safe deadman switch. Exposes read-only MCP tools (route_status, route_check, route_diff, route_trace) over stdio JSON-RPC; mutation is CLI/mTLS-REST only.
  commandFunction: |-
    (config) => ({
      command: 'agentic-route',
      args: ['mcp']
    })
  exampleConfig: {}
