# Smithery configuration: https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    description: Kernel policy-routing lock and net-safe deadman switch. Exposes MCP tools (route_status, route_check, route_enforce, route_monitor) over stdio JSON-RPC.
  commandFunction: |-
    (config) => ({
      command: 'agentic-route',
      args: ['mcp']
    })
  exampleConfig: {}
